export interface Product {
  upc: string;
  code: string;
  title: string;
  description: string;
  brand: string;
  model: string;
  category: string;
  lowest_price: number;
  highest_price: number;
}
